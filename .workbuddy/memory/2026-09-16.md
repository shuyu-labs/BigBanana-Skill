
## 2026-09-16 bigbanana-2.5 视频模型兼容
- scripts/bigbanana_video.py：新增 bigbanana-2.5（参考 D:/AI/shuyu/Code/BigBanana web/js/sora-api.js 协议）
  - 走标准 JSON 载荷：{model, prompt, duration:int, aspect_ratio, images[]}（区别于 2.0 的 size:"16*9"+seconds 字符串）
  - 时长固定 30s：MODEL_DURATIONS=[30]，run_generate 对非 30 请求自动校准（镜像 getValidDuration）
  - 多参考图上限 30 张（MAX_BIGBANANA_2_5_REFERENCE_IMAGES），纳入 DOUBAO_REFERENCE_MODELS（丢尾帧+注入 @N 注释）
  - 新增 STANDARD_JSON_VIDEO_MODELS 集合与 create_task_standard_json()；仅支持 16:9/9:16
- references/models.md、api_patterns.md 同步文档
- 验证：unittest 3 项全过 + mock dry-run（8s→30s 校准、36 张图截断到 30、注释注入、payload 形状）
- 坑：对同一文件并行发多个 Edit 会互相覆盖（部分编辑静默丢失），改单文件应串行执行

## 2026-09-16 导演分镜流水线移植（AI-Director -> skill）
- 对比 D:/AI/shuyu/Code/BigBanana-AI-Director services/ai/directorAgentService.ts（commit 52b019f/54cdb70）
- 新增 scripts/bigbanana_director.py：四阶段流水线（剧本分析/节拍规划/分场景镜头扩写/质检修订）+ 质量报告
  - 移植纯逻辑守卫：splitDirectorSource、trimDialogueNarration、对白正则兜底、角色ID不猜子串、
    enforceBeatCoverage 单调对齐、ensureBeatShots 补拍、sanitizePropBindings 道具时序、reconcileDialogues、allocateShotCounts、computeReport 评分
  - 输出 to_skill_shots 兼容项目 schema（shot_id/characters/start_frame_prompt/video_prompt），可直接接 run_assets/run_shots
  - 提示词模板用 @@token@@ 替换（str.format 与 JSON 花括号冲突）；媒体字段递归剥离 data: URL
- bigbanana.py 注册 director 子命令；SKILL.md/prompt_methodology.md/workflow_modes.md 同步
- tests/test_offline.py 增加 DirectorPipelineTests（8 项），11/11 全过；全流水线 mock dry-run 通过（覆盖率 100%，评分 99）
- 教训：AI-Director 的 prompt 模板默认值在 promptTemplateService 中，本次按 TS 内联硬规则浓缩，未逐字对齐模板默认文案

## 2026-09-16 修复：剧本 JSON 缺 scene id 导致导演流水线校验全挂
- 现象：agent 生成的剧本 scenes 只有 name 没 id，导演流水线按 scene.id 校验对白/节拍归属必然失败（截图 S09 会话复现）
- 根因（双缺口）：① SCRIPT_PROMPT 输出模板本身就没有 id 字段（生成端无约束）；② bigbanana_director.py cmd_direct 直接 json.loads，
  不像 workflow 那样调 normalize_script 兜底
- 修复：① SCRIPT_PROMPT 模板给 characters/scenes/props/shots 补 id（char_01/scene_01/prop_01/shot_01），shots 改用 id 引用；
  ② director 入口加载后 normalize_script，若有补 ID 则写回 script.json 保持磁盘一致；
  ③ SKILL.md 标准流程第 1 步 + director 命令注释写明 id 约束
- 验证：11/11 离线测试过；normalize 模拟 name-only 剧本补 ID 且 scene_id 映射正确；SCRIPT_PROMPT format 渲染无花括号问题
- 注意：tests 需要 Pillow，托管裸 python 没有，用 envs/default venv 跑

## 2026-09-16 新增「步骤确认门禁」（用户明确要求）
- SKILL.md 新增「步骤确认门禁（强制）」：剧本拆分 → 分镜 → 角色造型/场景/道具 → 首帧 → 视频 → 配音/导出，
  每步完成后必须停下展示产物、取得用户明确确认才能进下一步；不允许连跑多步或用"有问题再告诉我"代替确认
- 付费远程生成（图片/视频/配音）调用前须说明模型、数量、预计消耗并确认；run --approve 不豁免步骤确认
- workflow_modes.md 同步：workflow run 内部每完成一个阶段就停下等确认
- 偏好来源：用户要求对产线每步人工把关（他只保留剪辑那道工序的习惯延伸，其余工序他要看点）

## 2026-09-16 九宫格协议修正（单图网格，非逐格生成）
- 用户指出：九宫格 = 一张图内含九格（一次图像调用），不是 9 张单图；旧 skill grid 命令逐格渲染 9 次付费生成
- 对照 AI-Director 源码确认：shotService.ts NINE_GRID_IMAGE_PROMPT_TEMPLATE + cropPanelFromNineGrid + storyboardPromptTemplates
- bigbanana_generate.py：GRID_PROMPT 重写为视角拆分协议（index/shot_size/camera_angle/description 英文10-30词、
  组合去重、≥3 景别、首格建场末格结果），resolve_grid_layout(4/6/9，9:16 六格 3x2)，validate_grid_panels + 自动纠偏重试一次
- bigbanana_visual.py：grid 改为单图生成（prefix+逐格描述+suffix+负向词+无文字规则，--ref 作连续性锚点）；
  新增 grid-crop 本地裁格（PIL 按行列定位，6 格竖图自动推断 3x2）；旧版 composition_prompt JSON 明确报错要求重新生成
- 测试 +4（GridProtocolTests），15/15 全过；SKILL.md / visual_quality.md 同步
- 坑：断言 "Panel " 计数时别忘 prompt 里还有 "Panel order:" 行
